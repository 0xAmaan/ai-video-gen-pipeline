/**
 * Filters Module
 *
 * Provides filter presets and configurations for video effects.
 */

export {
  type FilterPreset,
  FILTER_PRESETS,
  getPresetById,
  getPresetsByCategory,
  createEffectFromPreset,
} from "./presets";
